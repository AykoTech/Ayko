version: '3.8'

services:
  ayko:
    build: .
    container_name: ayko-ai
    stdin_open: true
    tty: true
    volumes:
      - ./config:/app/config
      - ~/.ayko:/root/.ayko
    environment:
      - AYKO_OFFLINE_MODE=true
      - AYKO_LOG_LEVEL=INFO
    restart: unless-stopped
